# This rule is not used by the project kubeflare-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the cluster.kubeflare.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kubeflare-operator
    app.kubernetes.io/managed-by: kustomize
  name: cluster-cluster-editor-role
rules:
- apiGroups:
  - cluster.kubeflare.com
  resources:
  - clusters
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - cluster.kubeflare.com
  resources:
  - clusters/status
  verbs:
  - get
